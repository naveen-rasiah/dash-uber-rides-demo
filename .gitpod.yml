ports:
  - port: 8050
    onOpen: open-preview

tasks:
  - init: |
      virtualenv .venv
      source .venv/bin/activate
      pip install -r requirements.txt
    command: |
      source .venv/bin/activate
      python app.py